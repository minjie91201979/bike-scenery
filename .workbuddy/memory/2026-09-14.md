# 2026-09-14

## 完成：各国特色店铺卡通素材生成器

- 在 `tools/shop-generator/` 下创建了完整的参数化批量生成工具：
  - `countries.mjs`：12 个国家的建筑 / 文化符号 / 配色 / 光照配置数据层。
  - `style.mjs`：统一的卡通画风底座（粗黑描边、饱和色、正面店铺立面、无文字水印）。
  - `generate-shops.mjs`：CLI 批量生成器，支持 `--country`、`--all`、`--dry-run`、`--size`、`--concurrency`，并发调用 Agnes Image 2.0 Flash。
  - `make-gallery.mjs`：自动把 `output/` 下图片拼成深色科技风的本地 `gallery.html` 预览。
  - `package.json`、`README.md`。
- 已实际跑通 12 张图（法/日/墨/意/中/英/美/荷/希/印/摩洛哥/泰），全部落盘到 `tools/shop-generator/output/`，并成功生成 `gallery.html`。
- 该工具可直接作为游戏资产管线使用：新增国家只需在 `countries.mjs` 追加配置即可复用整套风格与生成逻辑。

## 完成：店铺 3D 模型生成（腾讯混元 3D）并存入游戏资产目录

- 用 `3D模型与视频特效` skill（腾讯混元 3D，脚本 `buddy-cloud.py`）把店铺做成 LowPoly GLB，落盘到游戏资产目录 `public/assets/models/`（游戏经 `/assets/...` 加载，目前无模型加载器，需后续接 GLTFLoader）。
- 编排脚本 `tools/shop-generator/gen3d.py`：先 `--no-poll` 提交全部任务（秒级拿到 job_id），再并发用 `status` 命令轮询到 DONE，然后下载 `ResultFile3Ds` 里的 GLB，并生成 `viewer.html`（model-viewer 预览，扫描目录内所有 `*_shop.glb`，可增量补生成）。
- 已成功产出：**法国咖啡馆 fr_shop.glb（15.8MB）**、**日本拉面店 jp_shop.glb（15.8MB）** + 各自 `_preview.png` + `viewer.html`。
- ⚠️ 关键坑：本机该 API 维度的**免费额度是 5 次/天提交**（报错 `daily submit limit exceeded (5/5) for dimension hy-3d`），不是网站写的 30 次/天。一次跑 4 国 + 之前测试已用尽，导致墨西哥/中国当天无法提交，需等次日额度重置后再跑 `python3 gen3d.py <token>`（SHOP_FILTER 不支持，直接整批即可，已生成的会复用/跳过逻辑需自备；当前脚本对已存在 glb 不会重复生成）。
- 产物较大（40000 面 GLB ~15MB），移动端若偏大可调低 `--face-count`（如 20000）或改用 `--generate-type Geometry` 白模。

## 完成：3D 模型接入游戏引擎（RoadShops.ts）

- 重写 `src/engine/RoadShops.ts`：用 `GLTFLoader` + `DRACOLoader`（gstatic 1.5.7 decoder）加载 AI 生成的 GLB；`GLB_MAP` 登记已生成国家 `france:'fr' / japan:'jp' / mexico:'mx' / china:'cn'`；`spawn()` 按 packId 决定加载 GLB 还是回退到原有程序化 `makeShop`；`addGLB()` 克隆、居中 X/Z、落地、按路心旋转，并 push 进 `world.shops` 用于碰撞。
- `npx tsc --noEmit` 通过（EXIT 0）。mx/cn 虽尚未生成，但 `GLB_MAP` 已登记，GLB 落盘后引擎自动启用，无需再改代码。

## 状态：墨西哥/中国 3D 模型被 API 配额卡住（次日自动补）

- 今日（9/14）混元 3D 提交配额 5/5 已用尽，mx/cn 提交直接报 `daily submit limit exceeded (5/5)`（失败的提交不消耗配额，次日重置满血 5 次）。
- 已增强 `gen3d.py`：新增与 SHOPS 解耦的 `NAME_MAP`，使 `SHOP_FILTER=mx,cn` 补生成后 `viewer.html` 仍显示全部四国中文名；并支持 `SHOP_FILTER` 只跑部分国（避免重提已完成的 fr/jp 浪费配额）。
- 已建立**一次性自动化** `automation-1789355604704`（名称"补齐墨西哥/中国 3D 店铺模型"， scheduledAt `2026-09-15T09:30:00+08:00`，ACTIVE）：到点自动 `connect_cloud_service` 取新令牌 → `SHOP_FILTER=mx,cn python3 gen3d.py <token>` → 校验 `mx_shop.glb`/`cn_shop.glb` 落地。失败则停止不重复提交。主人也可明天直接让我跑。
- 确认过 token 本身有效（到期 2026-09-15 01:42 UTC+8），429 是外部配额硬限制，无法绕开。
- 主人于 11:19 重申"补齐所有模型"，再次测试仍被 5/5 配额卡住（失败的提交不消耗，额度在次日零点重置）。自动化已兜底，无需再跑。

## 完成：本地生成 faceted low-poly 3D 店铺模型（零外部依赖）

- 创建 `tools/shop-generator/gen_all_shops.py`：纯 Python + struct 手写 GLB 二进制，不依赖 Blender、混元 API、或第三方 npm 包。
- 支持 12 国店铺造型（法/日/墨/中/意/英/美/荷/希/印/摩洛哥/泰），每国独立生成函数。
- 已生成全部 12 个 GLB 到 `public/assets/models/`，总计 **61 KB**（AI 生成模型每个 15 MB，压缩比 250:1）。
- 重建 `viewer.html`（深色 UI，下拉切换模型）和 `all-shops.html`（12 国卡片网格，上 3D 下 2D）。
- `RoadShops.ts` 的 `GLB_MAP` 已登记全部 12 国，`npx tsc --noEmit` 通过。
- **用法**：`python3 gen_all_shops.py [id 列表]`，不加参数生成全部。
- **技术对比**：
  - 混元 3D API：高质量艺术风格，但 5 次/天配额，任务慢（10-30 分钟）
  - 本地生成（faceted low-poly）：零配额限制，秒级出图，可无限迭代，造型简化但适合游戏
